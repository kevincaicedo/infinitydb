# InfinityDB Claim Ledger

Master plan §19 made operational (M1-S16). **Every number or comparative
statement in public copy must have a row here with status `Allowed`, citing
its artifact.** A claim absent from this ledger does not exist. Statuses:

- **Allowed** — artifact exists, run validity rules held, claim may ship.
- **Narrowed** — a weaker form is allowed; the allowed wording is in the row.
- **Rejected** — evidence contradicts it or the run was invalid.
- **Evidence-pending** — no qualifying artifact yet. Treat as Rejected for
  public copy.

Run-validity rules (constitutional, §19): 3–5 replicates · clean tree ·
governor/EPP recorded · thermals monitored · load-gen saturation disposition
· tripwires green in the same run · competitors on the same box and workload
· flamegraph + memory attribution attached. **Dev-box runs can never back a
performance claim** — only the Linux reference box (M0-R2) can. Correctness
claims (byte-compat, determinism, test counts) may cite dev-tier runs, with
the environment named.

Re-validation: every release re-runs the artifact behind each `Allowed` row;
a row whose artifact has not been re-validated for the release being
announced reverts to Evidence-pending for that announcement.

## Review checklist (release manager, per announcement)

1. Extract every number/comparative from the draft announcement.
2. Each must match an `Allowed` row's wording — narrowings are not optional.
3. Each cited artifact must be from the release's own re-validation run.
4. Tripwires in the citing run must be green; otherwise the row flips.
5. Sign the announcement with the ledger commit hash it was checked against.

---

## Rows — v0.1.0-alpha.1 (M1)

| # | Claim (allowed wording) | Status | Artifact | Notes |
|---|---|---|---|---|
| C1 | "Serves the Redis protocol: 65 commands declared — 47 `full`, 15 `partial` with documented deviations — byte-diff-verified against Redis 8.0.5 (378 byte-compared cases, 32 documented deviations, 0 failures)" | **Allowed** | [`docs/compat-matrix.md`](compat-matrix.md) (generated; CI staleness gate) + `cargo test -p compat` run 2026-06-12 vs local redis-server 8.0.5 | Correctness claim — dev-tier oracle is valid evidence. CI re-runs against the pinned `redis:8.0.5` container. Do **not** round up to "~90 commands". |
| C2 | "Any Redis client library works unchanged" | **Allowed 2026-08-16** → "redis-py, node-redis, go-redis and lettuce pass a smoke suite in CI" | CI `client-smoke` job (infinity-ci.yml) | **Dispositioned-bound (S19 walk, 2026-07-10): bound to the named S02 blocker** — the self-hosted runner registration (user task); flips Allowed on the job's first green run. Not a parking lot: the blocker, owner, and flip condition are named. **Flipped from Narrowed 2026-08-16:** the `client-smoke` job ran green in `infinity-ci` run **31926998599**, serving all four clients sequentially against one `infinityd` (redis-py, node-redis, go-redis, lettuce — workflow lines 196–251). It runs on a GitHub-hosted `ubuntu-24.04` runner, so the S02 self-hosted-runner blocker never applied to this row; that blocker still binds **C9** (release-pipeline dry run). Do **not** widen the wording to "any client library" — four named clients is what was measured. |
| C3 | "Deterministic-simulation tested: same seed, byte-identical traces; per-key linearizability, TTL, pub/sub delivery, and accounting oracles run nightly" | **Allowed** | `cargo test -p inf-sim` + `inf-sim --verify-determinism` runs 2026-06-12 (m0-smoke hash `0xc038aae0837a1995`); nightly fleet: infinity-dst-nightly.yml | Correctness claim. "≥ 1M simulated seconds nightly" only after the nightly job's first green run. **M2.5-S14 deepened the fleet** (device-stall model → everysec-deferral oracle; combined durable+memory+pub/sub+expiry scenario → L2 memory-volatility/log-scan oracles; boot-storm promoted in), each with a demonstrated planted-bug catch (`m2.5/s14-dst-20260707/`); the *deepened* fleet's nightly-live evidence is still pending S02's runner — do not claim the new scenarios "run nightly" until the first green run. |
| C4 | "10M TTLs across 48 simulated hours fire exactly (zero early, zero missed)" | **Allowed** | `INF_DST_FULL=1 cargo test --release -p inf-store --test expiry` (2026-06-12, dev box, 6 s) | Correctness claim, virtual time. |
| C5 | Any throughput number (ops/s, vs Redis, vs Dragonfly) | **Narrowed** → binding M0 + M1 rows exist and are citable with box + no-turbo tier disclosed | S19 closure campaign 2026-07-10 (`--reference-box`, env-check OK): `.artifacts/m0/1783698386-gate-run/` (4 cells, n=5): unpipelined **3.21× Redis** (512 conns); pipelined natural 2.81–2.89M / all-local **8.03M**; anchor **1.72×** Dragonfly in-run · `.artifacts/m0/1783698588-gate-run/` (8 cells, n=3): natural 3.92–4.02M / all-local **11.15M**; anchor 1.41×; unpipelined 4.21× Redis · `.artifacts/m1/1783699242-gate-run/` (M1 rows bound: baseline pipelined 2.79–2.83M, eviction-pressure 2.21M, TTL-mix 2.47M) | Citable per row with "i7-13700KF, N cells, turbo off" disclosed. **8-cell caveats travel with the 8-cell rows:** the `sqes/submit` tripwire read red (7.8–9.5) on that run — quote its rows as shape-caveated; the pinned spec-shape supplementary read (`m2.5/s19-closure-20260710/phase2-8cell/`) gives natural 5.08–5.57M with loadgen on E-cores and its all-local generator-floored at ~7.0–7.1M (ceiling measured 7.07M, phase0). The ≥ 6M pipelined-gate verdict binds on the natural row — red at both shapes, dispositioned by ADR-0029 → ADR-0035; never publish a natural number without the penalty context. Supersedes the 2026-07-06 rows (`1783391461` +2). **S24 phase-2/3 re-validation (2026-08-11, binding):** wedge re-proven in-run — inf-compare `1786420407` unpipelined 754–782k vs redis 265–287k (≈ 2.7–2.9×) and ≈ 1.3–1.4× dragonfly; m0 `1786419995` pipelined natural 2.844M (July reproduced), anchor 1.68× (both binaries, same night). Environment shift disclosed: the m0 memtier-512 unpipelined ratio reads 2.7–2.8× tonight *for both the m4 and the a1ebcb9 baseline binaries* (redis unchanged) — the July 3.21× is box/generator-state-specific; S25 re-signs wording against the fresh artifacts. |
| C6 | Any latency number (p99/p99.9 under storm, FLUSHALL-under-load, fan-out < 5 ms) | **Narrowed** → the M0 memtier p99.9 row and the full M1 latency row set bind green: expiry storm drains 0.70 s with reads-through-the-storm p99.9 911 µs; FLUSHALL-under-load read p99 607 µs (the flush itself 7.7 ms at 2M keys); pub/sub fan-out PUBLISH p99 0.75 ms to 512 receivers | `.artifacts/m1/1783699242-gate-run/` (S19 closure campaign 2026-07-10, `--reference-box`) + `.artifacts/m0/1783698386-gate-run/` (memtier p99.9 row) + the S10 storm replicates (`m1/1783439386` +2: 0.44–0.49 s / 799–863 µs) | Historic 11.0–11.4 s "drain" numbers are **retracted as measurements** — the harness poll floor (S10; `m2.5/s10-expiry-drain-20260707/results.md`); never quote them as drain times. Two rows stay red-informational, disclosed wherever latency is quoted: KV p99.9 under pub/sub background 2.88 ms vs the 2 ms bar (pre-existing) and the TTL-heavy feature-pressure mix p99.9 2.11 ms vs 2 ms. |
| C7 | "RSS ≤ 1.0× Redis on 10M × (16 B, 64 B)" | **Allowed** → allowed wording: "0.61× Redis RSS at 10M keys × (16 B, 64 B), both engines filled in the same run on the same box" | `.artifacts/m0/1783698386-gate-run/` + `.artifacts/m1/1783699242-gate-run/` (S19 closure campaign 2026-07-10, `--reference-box`, env-check OK; both runs read 0.611×) | Closed by S19. The 8-cell shape reads 0.657× (`m0/1783698588`) — still comfortably under 1.0×; quote the 4-cell 0.61× as the primary number with cells disclosed. **Re-validated 2026-08-11 (S24 phase 2, binding): 0.61× again on the m4 tree** (`m0/1786419995` + the same-night baseline run read identically; compare `1786420407` bytes/key 88.4 vs July 88.3). |
| C8 | "Eviction hit-rate parity with Redis LFU (±2 pp, zipfian)" | **Allowed** → allowed wording: "LFU hit-rate parity with Redis under a zipfian trace: 96.19% vs 96.19% (+0.00 pp), 1M keyspace, 5M ops, same trace both engines" | `.artifacts/m1/1783699242-gate-run/` (S19 closure campaign 2026-07-10, binding) | Closed by S19 — exact parity, well inside ±2 pp. **Re-validated 2026-08-11 (S24 phase 2, binding): 0.00 pp again on the m4 tree** (`m1/1786420253`). |
| C9 | "Docker image < 30 MB, serves redis-cli out of the box" | **Evidence-pending — bound to the named S02 blocker** (S19 walk, 2026-07-10) | — | infinity-release.yml measures/gates this on the release-pipeline dry-run, which needs the S02 runner (user task); flips on that run. Dev-box build: 0.8 MB image, serves redis-cli **with the bundled `deploy/seccomp/infinitydb-seccomp.json` profile** — io_uring requires it under Docker's default seccomp, so "out of the box" carries that documented requirement (see docs/deployment.md). |
| C10 | "A slow pub/sub subscriber is disconnected at the configured output cap, never bloats the server" | **Allowed** | `node_e2e::slow_subscriber_hits_the_output_cap_and_dies` + `gate-run m1` slow-subscriber row (killed=true, dev run 2026-06-12) | Correctness/behavioral claim. |
| C11 | "Memory accounting is exact: per-domain attribution reconciles, divergence from RSS > 10% fails CI" | **Allowed** → allowed wording: "per-domain memory attribution reconciles against RSS in every binding gate campaign — 2.7% divergence on the 10M-key memory fill, 1.0% on the durable fill (log domains included); > 10% fails the gate" | `.artifacts/m0/1783698386-gate-run/` (2.7%) + `.artifacts/m2/1783699515-gate-run/` (1.04%, gate row PASS) — S19 closure campaign 2026-07-10 | Closed by S19: the divergence gate ran binding on the reference box in both campaigns. |

## Rows — v0.2.0-alpha.1 (M2)

Box for every performance row: the **user-designated reference box**
(ADR-0022 D1) — HomeLab i7-13700KF, pinned P-cores, `performance` governor,
**ADATA LEGEND 700 consumer Gen3 DRAM-less NVMe** (§19 profiles Gen4; the
deviation is named in each row). Campaign artifacts:
`infinitydb/.artifacts/m2/` (2026-07-05, clean tree, env-check green).

| # | Claim (allowed wording) | Status | Artifact | Notes |
|---|---|---|---|---|
| C12 | "Durability is deterministically tested: 10,000 simulated power-cut/disk-fault seeds, zero durability-oracle violations — every `always`-acked write survives, `everysec` loses at most one second (simulated time); every seed replays byte-identically — and on the frame-v2 tree, zero recovery refusals across the same 10,000 seeds" | **Allowed** (re-signed against the v2 tree, S19 2026-07-10) | `m2.5/s12-frame-seq-20260709/` (frame v2: 0 refusals / 0 violations at 10k identical seeds, paired against the re-measured 2.31% v1 baseline) + `dst-sweep-10k-s22-20260705/` (the release-code regeneration) | Correctness claim. The old 1.33% legal-refusal disclosure is **superseded by frame v2** (ADR-0031) — the honest historical note stays in the artifact, not in public copy. |
| C13 | "The crash matrix — 8 named fault points × fsync policies × workloads, kill-and-recover with digest verification — is green at 256 seeds per combination; fsync failure is fail-stop" | **Allowed** (re-validated post-format-change: S12 re-ran the matrix green on the frame-v2 tree, 2026-07-09) | `m2.5/s12-frame-seq-20260709/` (v2 tree) + crash-matrix run 2026-07-05 + `tests/crash-matrix/m2.toml` | Correctness claim. |
| C14 | "A 10 GB node (8 cells) cold-boots to serving in under 10 seconds on the reference box (9.8 s, cold page cache, 3 replicates < 1% spread)" | **Allowed** | `parallel-boot-cold-20260705/` + the S08 re-run (7.3 s, `m2.5/s08-readahead-20260706/`) | Say "under 10 s measured / 15 s gate"; device is Gen3 DRAM-less (named). Release re-validation (cold-boot harness re-run) rides the S04 pre-tag campaign — flagged at the S19 walk. |
| C15 | "Memory namespaces pay zero for durability: A/B vs the pre-durability build shows ≤ 1% on every gate mix (worst 0.63%, p99.9 same-bucket-or-better), and memory-only runs append zero log records (report-enforced)" | **Allowed** | binding report `.artifacts/m2/1783232863-gate-run/` (A/B halves) + `.artifacts/m2/1783699515-gate-run/` (zero-log-records half re-validated S19 2026-07-10, gate row PASS) | Signed deltas + LogHistogram ~3% quantization in-report; one retained repeat showed ±1–2-bucket latency drift (disclosed there). **S04 re-validation item:** the A/B halves need the pre-M2 baseline binary rebuilt (`--baseline-bin`) in the pre-tag campaign — flagged at the S19 walk. |
| C16 | "Checkpoints are fork-free and memory-flat: continuous checkpoint cycling under a saturating durable write mix costs ≤ 15 MiB of peak RSS vs a no-checkpoint control (a fork/COW rewrite would cost the dataset)" | **Allowed** (re-validated S19 2026-07-10: pressure RSS 239–241 MiB vs 235–238 MiB control, 68–88 ckpt cycles/rep, gate row PASS) | `.artifacts/m2/1783699515-gate-run/` + S22 binding report (`ckpt_rss_overhead` 14.4 MiB, 176 cycles in-row) | The anti-BGREWRITEAOF *memory* claim. One pressure rep logged 1,595 request errors of 17.5M ops (0.009%) — disclosed; the control's own p99.9 excursions (36–426 ms) are the C17 device class. |
| C17 | "Checkpoint-under-load foreground p99.9 < 2 ms" | **Narrowed** → "checkpointing adds no measurable foreground p99.9 over the no-checkpoint control on our reference device, and holds p99.9 ≈ 1.1 ms under 324 checkpoint cycles at the loop tier (tmpfs isolation row)" | S12 tmpfs row (`1783049677-gate-run`) + S22 device rows | Absolute 2 ms bar on the Gen3 device: the *control itself* stalls 36–45 ms (device class; the S19 re-read reproduced it — control p99.9 excursions to 426 ms with checkpoints **off**). Evidence-pending (Gen4) for the absolute wording — ADR-0022 D4.3; **dated plan: S18 decision memo delivered 2026-07-10 (`m2.5/s19-closure-20260710/runbook.md`), user decision due at the S04 tag.** |
| C18 | "`always` group commit: ≥ 300k grouped writes/s with honest fsync histograms" | **Evidence-pending (Gen4 device)** | binding report `1783232093`/`1783232863` (128–156k w/s post-fix, ratio 100, fsync p50 2.5–3.1 ms attached) + `redis-aof-context-20260705` (Redis AOF-always 373k same box/shape) + driver-tier 778k @ group 1024 (same device) + tmpfs full-stack 2.34M + M2.5-S07 A/B `m2.5/s07-ab-20260706/verdict.md` (formation instrumented: group p50 = arrivals-during-flush at ~100% collection efficiency; two-in-flight pipeline Rejected, −3.7% w/s, fsync p99 +37%) | Do not publish an `always` throughput number yet. The citable story is the mechanism: one durability fsync in flight (ADR-0022 D3, re-validated by the S07 A/B — pipelining *splits* groups on this device) collecting every arrival during the flush (group p50 ≈ per-cell rate × fsync p50, measured 101 vs 97 predicted). D8.6's dead-time premise is Rejected (ADR-0026 D4); the 300k absolute is owned by S18/Gen4 (flush latency and arrival rate move together). **Dated plan: S18 decision memo delivered 2026-07-10 (`m2.5/s19-closure-20260710/runbook.md`), user decision due at the S04 tag.** |
| C19 | "everysec penalty < 10%" | **Evidence-pending (status corrected 2026-08-16).** The former `Narrowed` wording ("≈ 10–11%") is **withdrawn**: readiness F29 showed the row is *bimodal*, not drifting, so no single percentage describes it. **Nothing about `everysec` cost is quotable for this tag.** The "< 10%" absolute stays Evidence-pending (Gen4 device). *The status field still read `Narrowed` while this row's own notes said the wording reverts — the notes were right; a reader scanning the status column would have quoted a withdrawn number.* | `.artifacts/m2/1783699515-gate-run/` (10.1–10.9%, memory-ns 2.07M vs everysec 1.84–1.86M) — supersedes the M2-era 30–45% device-bound reading | The Phase-P/H levers moved this row from 30–45% to ~10.5%: the log stream no longer saturates the device at the higher throughput's duty cycle. The < 10% gate misses by ~0.1–0.9 pp on Gen3; re-read rides the S04 pre-tag campaign and S18/Gen4 (ADR-0022 D4.2 disposition narrows accordingly). **NOT re-validated by the S24 phase-2 leg (2026-08-11): the row read 33.6% on the device** (everysec 1.38–1.65M, p999 3–14 ms; `m2/1786421205`) while the same binary the same night on tmpfs read 1.96–1.99M — the delta is drive state (DRAM-less SLC at 40% fill; readiness F20), not the code. **This wording reverts to Evidence-pending for the v0.4.0 announcement** unless a post-fstrim re-read restores it; do not quote ≈ 10–11% until then. **RESOLVED 2026-08-15 (readiness F29): the post-`fstrim` re-read did NOT restore it, and the row turns out to be *bimodal* rather than drifting.** Two `gate-run m2` legs ~1 h apart, same binary, same night, read **67.59%** and **8.24%**; pooling their six everysec replicates gives 397k, 440k, 769k | 1607k, 1917k, 1960k — a 3/3 split across a 4.8× gap, while the memory-mode controls hold 2.04–2.38 M. An idle device probe between them (4 KiB `O_DIRECT|O_DSYNC` ×3) read 1.33 ms/write stable to ±2%, so there is no post-TRIM transient to wait out. July's 10.1%, August's 33.6%, and both of tonight's readings are the same process sampled three times each. **A 3-replicate median cannot instrument this row, so no single narrowed percentage is honest. C19 stays `Evidence-pending`; it is not restorable by re-running this instrument.** The honest fixes are a Gen4 device or a row that publishes the distribution instead of a median — recorded as owed. |
| C20 | "Recovery replays ≥ 1 GB/s per cell" | **Evidence-pending for v0.4.0 (status corrected 2026-08-16).** The narrowed wording "≥ 1 GB/s per cell on the steady-state (checkpoint + tail) boot shape, cold" is **not re-validated for this tag** and may not be quoted | `m2.5/s08-readahead-20260706/` (ick-tail cold 1.07–1.09 GiB/s = 1.15–1.17 GB/s, 5 replicates × ABBA legs, digests identical; tail-only full-log worst case 0.91–0.92 GiB/s = 0.98–0.99 GB/s) | Serial read∘apply is eliminated (ADR-0028: prefetch thread + `.ick` footer probe; WILLNEED Rejected by A/B) — cold = warm on both shapes. The worst-case row sits on this box's apply-CPU ceiling at no-turbo clocks; that ceiling, not I/O, is what a full-log-replay claim would need to name. Multi-cell boots keep prefetch off (regime split measured: 7.3 s vs 10.0 s; Gen4 re-eval at S18). Release re-validation (replay harness re-run) rides the S04 pre-tag campaign — flagged at the S19 walk. **Release re-validation 2026-08-16 — reverts to `Evidence-pending` for the v0.4.0 announcement.** This row claims the *steady-state* (checkpoint + tail) boot shape; the S24 campaign measured only the **worst-case tail-only** shape, warm cache, and read **0.266 GB/s/cell** (C38b, gate not met). The steady-state shape was **not re-run for this tag**, so the §19 rule applies and the ≥ 1 GB/s narrowed wording may not be quoted. Note the tension a reader must not paper over: C20 (M2-era, no tiering, steady-state) and C38b (M4, tiering on, tail-only) are the same gate in different shapes and currently disagree by ~4×. The measurement that settles it is the `ick-tail` leg of `infinitydb/scripts/recovery-brackets.sh`; until it runs, neither shape supports a public replay-throughput number. **Same-session measurement 2026-08-16:** the steady-state (`ick-tail`) shape now has a real number — **12.308 s** boot, 3.364 GB / 10.61 M records replayed, **~0.068 GB/s/cell on the whole-boot basis** (`infinitydb/.artifacts/m4/s24/recovery-brackets/`). That does not support the ≥ 1 GB/s narrowed wording on the whole-boot basis. On the *replay-phase* basis it is ~1.0 GB/s/cell — the same definitional split that C38b now turns on. This row and C38b must be dispositioned together, in one decision, or they will disagree again. **DISPOSITIONED 2026-08-16 with C38b, as F33 required (ADR-0070 D7).** The split resolves the disagreement: C20 was claiming ≥ 1 GB/s/cell on the *steady-state boot shape*, i.e. a whole-boot figure, which is now known to be dominated by `Phase::Start`. Under the split, whole-boot throughput is **not a gated quantity at all** — the operator-facing promise is the 15 s boot gate (C38a, passing) and the startup half is C38f (failing). **C20 is therefore retired as a claim rather than carried**: its wording measures something the project no longer gates, and the two rows that replace it are both live. Do not quote it. |
| C21 | "Memory attribution stays exact with the durable plane live: sum(domains) — including log staging and checkpoint buffers — within 1.1% of RSS on a 1 GB durable fill" | **Allowed** (re-validated S19 2026-07-10: 1.04%) | `.artifacts/m2/1783699515-gate-run/` attribution row (supersedes the S22-M2 artifact for re-validation purposes) | |
| C22 | "M0/M1 behavior carried: all M1 binding rows equal-or-better on the M2 tree (worst +1.5%)" | **Allowed** (re-validated S19 2026-07-10: the full m0+m1 gate sets re-ran binding on the v2+levers tree — every previously-green row green, throughput rows within ≤ 5% of the ADR-0033 baselines or better) | `.artifacts/m0/1783698386` + `.artifacts/m1/1783699242` (S19 closure campaign) | Carried disclosed failures: ~~M1 expiry-debt drain~~ (closed by S10 — instrument floor; see C6), M0-era absolute gates per ADR-0006/0007 → the pipelined-gate row is dispositioned by ADR-0029/ADR-0035. Red-informational rows named in C6. |

## Rows — v0.3.0-alpha.1 (M2.5 + M3, seeded 2026-07-17 by the S26 package; the S25 reference campaign re-validates every row before the tag)

| # | Claim (allowed wording) | Status | Artifact | Notes |
|---|---|---|---|---|
| C23 | "22 `JSON.*` commands declared — 7 `full`, 15 `partial` with documented deviations — byte-diff-verified against digest-pinned RedisJSON (Redis Stack 7.4.0-v8): 84 cases × RESP2+RESP3 = 168 executions, 148 exact, 20 justified deviations, 0 failures" | **Allowed** | `infinitydb/.artifacts/m3/s20-s21-20260716/redisjson-oracle.txt` + `s22-20260716/oracle-probes.txt` + generated `docs/compat-matrix.md` (JSON section, CI staleness gate) | Correctness claim — dev-tier oracle is valid evidence (environment named). Never round the surface up to "full RedisJSON compatibility". |
| C24 | "Documents are stored as a compact binary tape (`idoc` v1): 1.108× msgpack and 0.764× JSON text aggregate density on the reference corpus shapes" | **Allowed** | `infinitydb/.artifacts/m3/s02-idoc-20260710/` (S20 generator re-assertion) | Deterministic byte counts — hardware-independent; per-shape worst cases live in the artifact (small-200B 0.905× text, wide-array 1.119× msgpack) and travel with any per-shape quote. |
| C25 | "Document reads never run a JSON text parser — profile-proven: zero parser/path-compiler symbols under a live `JSON.GET` load" | **Allowed** (dev-tier proof; reference campaign re-proves via `scripts/check-doc-read-profile.sh`) | `infinitydb/.artifacts/m3/s25-dev-20260716/` (1,659 profile rows, mechanical grep = 0) | Correctness-of-architecture claim; the CI check makes regressions fail loudly. |
| C26 | "Every path mutation is one delta log record (0.066× full-document log volume on the 64-op mutation mix); 10,000 simulated document power-cut seeds: zero durability-oracle and zero replay-equivalence violations, replayed state byte-exact including lineage and version" | **Allowed** | `infinitydb/.artifacts/m3/s16-s17-20260712/` (log volume) + `s23-s24-20260716/doc-sweep-10k.txt` (30k equivalence checks, 150k documents compared) | Correctness claim, simulated time; the harness embeds fuzz-derived pathological documents (disclosed per seed). |
| C27 | "Document memory is attributed always-on (tape/tree/slack/path-cache domains, per namespace); attribution reconciles with RSS at 0.056% divergence on a 65k-document load (10% fails CI)" | **Allowed** | `infinitydb/.artifacts/m3/s18-s19-20260713/` | Correctness claim (Linux CI). Distinct from the *memory-gate* rows below. |
| C28a | "Reading one path out of a 1 KiB JSON document costs 1.13× a plain `GET` of the same-size string at p50 (1.44× at 200 B), against a ≤ 1.5× gate" | **Allowed** (binding, 2026-08-15) | `infinitydb/.artifacts/m3/wire-20260815-run{1,2,3}` — 3 runs × 3 internal replicates, `bench-m3-wire.sh` (memtier-labelled), env-check green, release tree | Medians of three runs: 1 KiB 1.1742 / 1.1348 / 1.0823; 200 B 1.4381 / 1.3846 / 1.4556. **Quote the 200 B number with its margin** — worst run 1.4556 against a 1.5 bar is thin, and a future regression there is a gate flip, not a rounding note. |
| C28b | Document write throughput ≥ 0.70× `SET` | **Rejected — gate not met** (binding, 2026-08-15; ADR-0070 D4) | same artifacts: **0.6817 / 0.6855 / 0.7172, median 0.6855** | The bar does **not** move and no ratio may be published. `JSON.SET` is stable to 0.2% across all three runs (1,277,475 / 1,279,560 / 1,277,475 ops/s) while plain `SET` spans 5% (1,781,283–1,877,042), so run 3's apparently-passing 0.7172 is a dipped denominator, not a faster document write; against the `SET` median the ratio is ≈ 0.69. The standalone throughput claim that replaces it is **C39**. Causal investigation is **M4.5-S20** — the ~0.31× gap is *not* attributed yet, so nobody may assert what it is. |
| C29 | "InfinityDB stores a mixed JSON corpus in 1.04× its serialized size and **0.38× RedisJSON's** resident memory" | **Allowed** (binding, 2026-08-15) | `infinitydb/.artifacts/m3/rss-20260815-{192646,192708,192715}` — 3 replicates, frozen corpus v2, `pipe_fnv1a64 = a2526081432de2bc` identical every run, DBSIZE verified on both engines | Measured 1.036 / 0.999 / 1.065 against a ≤ 1.5× bar and 0.379 / 0.365 / 0.389 against a ≤ 0.7× bar — wide margin on both. The corpus freeze (ADR-0046 D3) and the **write-once scope** note travel with any quote: RSS excludes post-ingest growth slack by construction. Per-shape rows are diagnostics only. |
| C30 | 24 h document soak (RSS slope < 0.5%) · fuzz ≥ 24 h/target | **Split 2026-08-15 — soak half Allowed, fuzz half Evidence-pending** | soak: `infinitydb/.artifacts/v0.4.0/soak-unified-20260814-0351/` (32.14 h, tree `82a8157`) · fuzz: nightly matrix, accrual restarted (readiness F23) | The document plane ran inside the v0.4.0 unified soak (ADR-0066 D1 — one node carrying cache/KV, document and tiered planes, which is a *stronger* form than the isolated `soak-m3.sh` run this row was written against, and the substitution is named per ADR-0025 D3). **Soak half: PASS** — RSS slope steady-state +0.234%/24 h and accounted −0.016%, `docs_live` 13,875 → 14,240 throughout, **6,024,814,768 document replies** over the run with zero errors on `doc_ingest`/`doc_read`. **Fuzz half stays pending**: F23 found 11 of 14 targets had banked zero hours (missing corpus dirs killed those jobs at checkout); the clocks restart from the fix landing. Do not quote this row as whole until the fuzz half closes. **Fuzz half quantified 2026-08-16 from the CI record.** `-max_total_time=19800` (5.5 h) per target per night, 14 targets. Green nights since the F23 fix: 08-14, 08-15, 08-15 (dispatch), 08-16 → **4 × 5.5 h = 22.0 h per target for the 11 formerly-broken targets**, against a 24 h/target bar — **short by 2 h; one more green night closes it.** The 3 committed-corpus targets (`manifest_decode`, `extent_decode`, `fuzz_tierfile_decode`) succeeded even inside the failed runs and stand at **9 × 5.5 h = 49.5 h**, well clear. Verified by job-level conclusions in runs 31666911952 (11 fail / 3 pass, pre-fix) and 31924203054 (14/14 pass). |

## Rows — v0.4.0-alpha (M4 tiered storage; drafted 2026-08-15, **walked and dispositioned 2026-08-16** after the S24 phase 4–7 campaign and the ADR-0070 owner decisions)

**Walk result: 4 gate rows are `Rejected` and say so.** C28b (document
write ratio), C34b (hot-set tail), C38b (recovery replay rate) and C38d
(cache isolation) were measured binding and did not meet their gates. They
stay in the ledger as rejected rows with their numbers, because a gate the
project failed is evidence too. Two rows were **split** so a passing half
cannot carry a failing half through a quote (C28a/C28b, C34a/C34b), and
C38 was split five ways for the same reason. One row (**C39**) was created
by ADR-0070 D4 to replace a rejected ratio with an honest standalone
claim. Rows whose artifacts were not re-run this campaign reverted to
`Evidence-pending` rather than inheriting an older number — **C38e was the
one such row, and it closed on 2026-08-16** when the storm benches were
re-run device-loaded at 3 replicates each (1.721 ms worst of six, against
a 2 ms gate). It is now `Allowed`, with the halved headroom disclosed
alongside it.


**Section rule:** every tiered *performance* number below is on a consumer
Gen3 DRAM-less NVMe (ADATA LEGEND 700, 476.9 GiB SKU) against a §19 Gen4
profile — the device disposition (ADR-0022 D4) travels with any quote, and
the Gen4 buy was deferred to M5 by readiness D8. `inf-bench` is the only
harness that can drive the 10× RAM zipfian tiered workload; memtier drives
the memory-mode surface only, and every row names its harness (ADR-0025 D3).

| # | Claim (allowed wording) | Status | Artifact | Notes |
|---|---|---|---|---|
| C31 | "A node carrying the cache/KV, document and tiered planes at once holds memory flat over a 24 h measured window: RSS slope +0.234%/24 h and accounted-memory slope −0.016%/24 h, against a < 0.5%/24 h gate, with zero crashes" | **Allowed** | `infinitydb/.artifacts/v0.4.0/soak-unified-20260814-0351/` (32.14 h = 8 h declared warm-up + 24 h window per ADR-0069 D1; tree `82a8157`; `verdict.txt` + `run-notes.md`) | The unified three-plane form (ADR-0066 D1), which is stronger than three isolated soaks and is the profile the release actually ships. Attribution residual +1.2%; end `smaps_rollup` **99.8% anonymous** (`Pss_File` 3.9 MB, `Swap` 0), so this is not a page-cache-inflated pass. Zero alerts. Quote the window form, never "24 h" bare without the warm-up disclosure. |
| C32 | "Compaction reclaims continuously under a 32 h endurance load: 9.59 M compaction slices with disk peaking at 38.73 GB of an 85.90 GB budget and *falling* to 30.30 GB by the end" | **Allowed** | same bundle — `samples.csv` (`compact_slices`, `disk_used_bytes` with 70 recorded decreases), `info-end.txt` | The reclaim claim rests on disk actually going **down**, not on a counter going up. Steady-window advance +9,072,308 slices — more than the 1024 MiB attempt 2's 6.50 M at double the memory budget. **Do not quote the verdict's "peak dead ratio" line**: readiness F24 — `tiering_dead_bytes` is cumulative and `tiering_live_bytes` static, so that ratio is not the engine's per-file trigger. |
| C33 | "Write amplification on the tiered plane stays at 1.89× of user bytes, worst namespace — against a < 3× gate" | **Allowed** (binding, 2026-08-15) | `infinitydb/.artifacts/m4/s24/ycsb/` and `ycsb-loaded/` — **1.89× on both legs**, load-independent as the counter-ratio definition predicts; the 32 h soak independently read 1.920× | Worst-namespace per ADR-0060 D1/D2/D4, never a node blend. Three independent runs at two different generator configurations agree inside 2%, which is what makes this quotable without a spread caveat.
| C34a | "On a node holding 10× its RAM, the hot set is served **faster** than the same workload on a fully RAM-resident node at the median: memory-hit p50 12–22% lower across three zipfian workloads" | **Allowed (median only — read C34b before quoting)** | `infinitydb/.artifacts/m4/s24/ycsb/` vs `ycsb-ref/`, same instrument, same campaign, one generator config (8 conns, pipeline 1); 3 rows compared, 7 named-excluded | Never publish this without C34b in the same breath. The p50 result is real and the mechanism is understood (phase-7 profiles show **no new hot-path symbol** on the tiered node), but a median-only quote would imply a tail that does not exist. |
| C34b | Hot set at memory speed: memory-hit p99/p99.9 within 10% of a RAM-resident reference leg | **Rejected — gate not met** (binding, 2026-08-15; ADR-0070 D3) | same artifacts — p99 **+328.21%**, p99.9 **+40.29%** (worst matched row) | The tiered tail is **workload-independent** (167–195 µs on every row) where the reference tail tracks its workload (39–155 µs): foreground commands queue behind cell-resident maintenance and I/O completion on a run-to-completion executor, the same mechanism as C38d. Carried to **M5** as research debt. Getting this gate to bind at all required three instrument fixes (readiness F25/F26) — before them it returned "0 rows matched" regardless of run quality.
| C35 | Cold-read p99 < 1.5 ms on NVMe under loaded zipfian rows | **Evidence-pending (Gen3-dispositioned; not claimable either way)** | **Load response measured binding 2026-08-15** on one node in one night: **1.44 ms** at 8 conns / pipeline 1, **3.65 ms** at pipeline 8 (`.artifacts/m4/s24/ycsb/` and `ycsb-loaded/`); 163–199 µs idle-drain (week 4); 12.8 ms unified soak attempt 2; 60.4–63.5 ms saturated 32 h soak | Ships as a **disclosed deviation**, never as a pass and never quietly omitted. The 1.44 ms reading is under light load and **is not the gate's condition** ("loaded zipfian, not idle drains") — quoting it alone would be the exact substitution §19 forbids. The honest public form is the range with its load levels attached. Gen4 device deferred to M5 (readiness D8).
| C36 | "The tiered plane served 154.1 M cold reads across 156 workload rows over 32 h with zero errors and zero nil responses" | **Allowed (correctness claim; dev-tier permitted, environment named)** | same bundle — 79 leg reports under `legs/`, `errors = 0` and `nils = 0` in every row | Correctness, not performance, so the Gen3 disposition does not gate it. Pair with C32 when describing endurance. |
| C37 | "Under sustained saturation the durable planes refuse admission explicitly rather than degrading: 100% of error replies are the typed `-BUSY` retry signal, at 0.55% of operations while sustaining 320,384 ops/s on a durable-`everysec` namespace" | **Allowed (correctness/behaviour claim)** | same bundle — `run-notes.md` §Disclosure 3, `loadgen-doc-summary.md`, per-leg `busy_retryable == errors` | This is a *backpressure-is-bounded* claim, not a throughput claim — the 320,384 ops/s figure is context for the refusal rate and must not be lifted out as a standalone performance number (it is a soak leg, not a gate row). |
| C38a | "A 10 GB tiered node restarts to serving in 5.9 seconds, against a < 15 s gate" | **Allowed** (binding, 2026-08-15) | `infinitydb/.artifacts/m4/s24/recovery/` + `recovery-analysis.md` — 10 GiB of user data in a tiered namespace, clean stop, timed to first `PONG` | Disclosed with it: this is the **worst-case shape** — no checkpoint had completed before the stop (`rdb_bgsave_in_progress:1`), so the boot replayed the full WAL tail with no `.ick` prefix to skip; and the page cache was warm, which can only make the boot faster. Both disclosures are conservative for C38b and generous for this row, so state them. **Reproduction attempt 2026-08-16 — the number does not reproduce.** The same shape, same box, same binary, same 10 GiB dataset, warm cache, 24 h later reads **13.415 s** (`infinitydb/.artifacts/m4/s24/recovery-brackets-tail-only/`). The `< 15 s` gate passes both times, but the margin falls from **2.5× to 1.11×**. The drive is the variable, not the code — the fill of the same dataset took 326–346 s today versus 110.9 s on 08-15 (the F20/F29 drive-state effect, now reaching the recovery gate). **Quote the gate verdict, not the 5.9 s figure**: "restarts well inside the 15 s gate" is supported; "restarts in 5.9 seconds" is a best-case reading of a row that moved 2.27× in a day. The worst-case-shape disclosure attached to this row **stands** — a same-session control put the checkpoint shape at 12.308 s versus 13.415 s tail-only, so tail-only is indeed the slower shape. **2×2 COMPLETE 2026-08-16 — the gate passes in all four legs.** Adding the cold-cache corner: **13.115 s** with `vm.drop_caches=3` (`Cached` 969 MB, `MemFree` 28.6 GB at boot), against **13.415 s** warm on the same shape in the same session — **cold is 2.2% faster, i.e. no effect**. Full set: 5.906 s (08-15 warm) · 12.308 s (ick-tail warm) · 13.415 s (tail-only warm) · 13.115 s (tail-only cold). **Allowed wording is the gate verdict plus the range**, e.g. "a 10 GB tiered node restarts inside its 15 s gate, measured 5.9–13.4 s across four legs on two days" — the single 5.9 s figure is **not** allowed alone, because the same shape read 13.4 s a day later and the variable is drive state. |
| C38b | Recovery replay ≥ 1 GB/s per cell with tiering on, **measured over the replay phase** | **Evidence-pending (instrument) — re-scoped by ADR-0070 D7, owner, 2026-08-16.** The row was previously `Rejected` at 0.266 GB/s/cell; that figure divided replayed bytes by *total* boot time and conflated replay with `Phase::Start`. Measured over the phase this row is named after, all four 2026-08-16 legs read **~1.0–1.2 GB/s/cell — at the bar**. It is **not** flipped to `Allowed`: 1-second progress polls can adjudicate a 2.4× miss but cannot certify a ~1 GB/s pass on a ~1 s phase. Binds when M4.5-S21 task 1 lands per-phase timestamps. **The startup half of the old row is now C38f, and it fails.** | same artifact — **0.266 GB/s/cell** (2,812,633 records/cell, 1.5695 GB, 5.906 s, four cells concurrently) | **Record-bound, not device-bound: 476 k records/s/cell against a device that reads sequentially at 3.6 GiB/s — ~13× below what the drive delivers.** No hardware purchase addresses this. Filed as **M4.5-S21** optimization work, the campaign's most important engineering finding per the owner. Owed: a steady-state `ick-tail` re-read and a cold-cache boot. **Re-read 2026-08-16 — the number is a misattribution and the row name does not match its method** (`infinitydb/.artifacts/m4/s24/recovery-brackets/ANALYSIS.md`). Boot logs carry per-phase progress, and in all three legs the cells sit in `Phase::Start` — *"Dirs, MANIFEST, scan, floor checks, checkpoint open + presize"* (`recover.rs:173`) — for nearly the whole boot, while record replay moves 3.4–6.3 GB inside about one second. Split at the observed phase transition, replay runs at **~1.0–1.2 GB/s/cell in every leg**, at or above the bar; the published 0.266 divides replayed bytes by *total* boot time, most of which replays no records at all. **So "record-bound, not device-bound" (F28) is mis-targeted**: the boot is `Start`-bound, and `Start` is directory/metadata I/O, which is device-state sensitive — which is why it swung 2.27× in a day. **Owner decision required before this row ships**: either the row means end-to-end boot throughput (then rename it and target `Start`), or it means replay throughput (then measure the replay phase). Precision caveat: the phase split is read from 1-second-granularity progress lines, so the per-phase rates are order-of-magnitude only and none is claimable until phase transitions are timestamped directly. **Cold leg adds a clean negative 2026-08-16:** dropping the page cache does **not** slow the boot (13.115 s cold vs 13.415 s warm, same shape same session), so `Start` is not re-reading data the cache would have held. Combined with the other two negatives — the boot is independent of replay volume (the 08-15 leg replayed the *most*, 6.278 GB, and was the *fastest*) and of boot shape (ick-tail ≈ tail-only) — the surviving hypothesis is write/metadata-side work inside `Start`, whose documented steps include *"checkpoint open + presize"*. A large preallocation on a DRAM-less SSD is both device-state sensitive and page-cache insensitive, which fits every observation. **Hypothesis, not a finding** — M4.5-S21 task 1 (phase timestamps) tests it. |
| C38c | "One node running a cache namespace, a JSON document namespace and a 10×-RAM tiered namespace at once accounts for its memory to within 6.5%, against a ≤ 10% gate" | **Allowed** (binding, 2026-08-15) | `infinitydb/.artifacts/m4/s20/1786833990|1786834800|1786835097-mixed-audit` — three runs, worst divergence 2.1–6.5%, with **816k–909k cold reads served in the mixed leg** | This is the first time the row was measured with the tiered *data* leg live — the harness previously carried only the control-plane half, and the leg had to be built (M4-S20, 2026-08-15). Page-cache disclosure: S09 chose `Direct` (ADR-0054) so tier reads bypass the page cache and no file-cache term is claimed against RSS. |
| C38d | Cache-namespace p99 within 10% of its solo baseline under the three-workload node | **Rejected — documented deviation** (binding, 2026-08-15; the S20 AC's own clause) | same artifacts — **+59.1% to +82.9%** across three runs; the July two-workload reading was +56.3% | Publish the mechanism with the number or not at all: cells are run-to-completion executors shared by all three namespaces, so a document serialization or a cold-read completion occupies the cell while cache GETs queue behind it. This is the unified profile's real cost model, corroborated a second way by the phase-7 profiles. Adding the third workload cost a further 10–25 points over the two-workload reading. |
| C38e | "Foreground p99.9 stays under 2 ms while demotion and compaction storms run" | **Allowed** (binding, 2026-08-16) | `infinitydb/.artifacts/m4/s24/storms/` — device-loaded (`INF_STORM_DIR` on NVMe, `TierIoMode::Direct`), `taskset -c 4`, **3 replicates each**: demotion **1.702 / 1.704 / 1.715 ms**, compaction **1.699 / 1.704 / 1.721 ms**. Worst of six binds at **1.721 ms**; `tail_alloc_stalls == 0` in all six | Re-run for *this* tag, so the release re-validation rule is satisfied and the July precedents are not quoted. The histogram is **conservative**: each `maintain()` slice's wall time is billed into the same vector as foreground ops (`demotion_storm.rs:167-172`), so maintenance cost is inside the number, not excluded from it. **Disclose with the claim:** headroom has halved since July — the same bench on the same substrate read 1.287–1.367 ms on 2026-07-18, so this is **+28% median-to-median** (+25% worst-to-worst) with no attribution, because no A/B was run to attribute it. State the margin as 14%, never as "comfortable". |
| C38f | "Startup overhead before replay begins stays under 5 s" | **Rejected — real defect, owner-classified 2026-08-16 (ADR-0070 D7)** | `infinitydb/.artifacts/m4/s24/recovery-brackets/ANALYSIS.md` — `Phase::Start` measured **10.8–12.5 s across four legs** against a **< 5 s** bar, a **2.2–2.5× miss** | Split out of C38b by the owner because the two halves are different promises and were giving opposite verdicts. `Phase::Start` is *"dirs, MANIFEST, scan, floor checks, checkpoint open + presize"* (`recover.rs:173`) and does no user-visible work, yet it consumes **~90% of a ~13 s restart**. Threshold derived, not fitted: one third of the existing 15 s boot budget. **Three mechanisms are already excluded by measurement** — it is not page-cache work (cold 13.115 s ≡ warm 13.415 s), not a function of replay volume (the leg that replayed the most was the fastest), and not a function of boot shape. It *does* track drive state 2.3× across days, so the surviving hypothesis is write/metadata work, most likely the checkpoint **presize**. **This is scheduled fix work (M4.5-S21), not a disclosure.** Precision: phase boundaries come from 1 s polls, which is ample to adjudicate a 2.4× miss. |
| C39 | "InfinityDB sustains ~1.28 M ops/s writing 1 KiB JSON documents on the reference box" | **Allowed** (binding, 2026-08-15; ADR-0070 D4 created this row when the ratio gate was rejected) | `infinitydb/.artifacts/m3/wire-20260815-run{1,2,3}` — 1,277,475 / 1,279,560 / 1,277,475 ops/s, a **0.2% spread**, `bench-m3-wire.sh` memtier-labelled | **This row exists because C28b failed, and it must never be presented in a way that implies C28b passed.** It is a throughput claim standing on its own evidence — do not place it beside a plain `SET` number, do not compute a ratio from it, and do not describe it as "close to" the gate. |
| C40 | "The tiered engine holds its safety invariants under fault injection: 25 crash- and disk-full-matrix rows green, and 20,000 randomized simulation seeds across two scenarios with zero never-none violations" | **Allowed (correctness claim)** | `cargo test -p crash-matrix` (25 rows, 0 failures, incl. the declared-fault-point coverage assertion) and `infinitydb/.artifacts/m4/s24/dst/` — `m4-recovery` 0/10,000 and `m4-tiered` 0/10,000 over 43.8 M commands with 10,000 DISKFULL refusals exercised | Correctness, not performance, so the Gen3 device disposition does not gate it. The DISKFULL count is the ENOSPC admission path being exercised deliberately, not a defect — pair it with C37 when describing refusal behaviour. |

## Standing obligations

- ~~**M0-R2** (reference-box campaign)~~ — **retired 2026-07-05** by the
  ADR-0022 D1 reference-box designation; the M0/M1 gate sets ran binding on
  the designated box inside the S22 campaign (C22). Device-bound M2 rows
  remain Evidence-pending on a Gen4 device (C17, C19, C20).
- **M2.5 owns closure (ADR-0023) — S19 walk executed 2026-07-10:** the
  former `Evidence-pending` set is dispositioned: **C7, C8, C11 → Allowed**
  (binding, S19 campaign); **C19 → Narrowed** at the measured ≈ 10–11%
  (the < 10% absolute stays Gen4-bound); **C5, C6 → Narrowed** with the
  full M0+M1+8-cell row set; **C12 re-signed** on the frame-v2 tree;
  **C17, C18 → bound to the dated S18/Gen4 plan** (decision memo
  delivered, user decision due at the S04 tag); **C2, C9 → bound to the
  named S02 runner blocker** (flip conditions stated in-row). Zero rows
  remain undispositioned; the release re-validation items for S04 are
  flagged in C14, C15, C20.
- **Cross-cell remote penalty (ADR-0025, owned by M2.5-S21 → S19):**
  binding trajectory: 60.4% baseline (2026-07-06) → 54.6–55.7% (ADR-0030
  levers) → ~64% after parse-batch grew all-local faster (ADR-0033's
  predicted arithmetic artifact) → **S19 8-cell re-reads 2026-07-10**:
  64.7% harness shape (tripwire-caveated), spec shape boundable-only
  (all-local generator-floored at the measured 7.07M E-core ceiling).
  The lever list is dispositioned to exhaustion (2 Accepted, 3 Rejected,
  1 inconclusive — ADR-0030/0033/0034); the **anchor holds with margin,
  1.72× (4 cells) / 1.41× (8 cells) Dragonfly in-run** — an architecture
  claim may cite it with the tier disclosed. Disposition: **ADR-0035
  (Proposed — user ratification)** re-expresses the staged gate as
  anchored gate + tracked ratio; the ≤ 25% real target stays carried
  (ADR-0027) with the pump-bypass restructure as its named structural
  lever. No ≤-40%/≤-25% claim either way.
- **Comparative instrument (ADR-0025):** from M2.5, comparative rows cite
  `inf-compare` artifacts (independent generators, competitors in-run,
  tier-bannered report). `inf-bench` rows prove gates; where a workload
  exceeds the external generators, the row names its harness — no silent
  substitution.
- **v0.2.0-alpha.1 release blockers (human tasks — now M2.5-E1):** 24 h soak
  (`infinitydb/scripts/soak-m2.sh`), first live-runner CI passes
  (fuzz-nightly hour, DST-nightly sweep, crash-matrix job, release
  pipeline on tag push), and the boot-wedge stop-item disposition
  (ADR-0022 D7).
- `v0.1.0-alpha.1` was never tagged (blocked on M0-R2) and is superseded by
  `v0.2.0-alpha.1`; its announcement draft stays unpublished. The M1-era
  correctness rows (C1–C4, C10) remain citable; C5–C9/C11 resolve under the
  designated-box rules going forward.

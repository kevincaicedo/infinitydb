# Allowlist for scripts/check-ledger-copy.py — one canonical token per line,
# format "<number> <unit>", lowercase, no thousands separators.
#
# RULE: a token may only be added here if
#   (a) an **Allowed** row in docs/claim-ledger.md covers the number, in
#       which case the comment names the row; or
#   (b) it is demonstrably NOT a performance claim (config example, wire
#       format bound), in which case the comment says so explicitly.
# Regenerate/review this file whenever the ledger changes.

# --- ledger C12 (Allowed): DST durability sweep -------------------------
# (the 1.33% legal-refusal disclosure was retired by frame v2 — ADR-0031;
#  C12 re-signed at the S19 walk 2026-07-10, site copy updated the same day)

# --- ledger C14 (Allowed): cold-boot recovery ---------------------------
10 gb           # C14: dataset size of the measured boot
10 s            # C14: "under 10 seconds" measured wording
9.8 s           # C14: measured boot time (3 replicates)
15 s            # C14: the gate value, disclosed alongside the measurement
1 %             # C14 replicate spread; also C15 "≤ 1% on every gate mix"

# --- ledger C15 (Allowed): memory namespaces pay zero for durability ----
0.63 %          # C15: worst-case A/B delta

# --- ledger C16 (Allowed): fork-free, memory-flat checkpoints -----------
15 mib          # C16: RSS-overhead bound vs no-checkpoint control
14.4 mib        # C16: measured value

# --- ledger C21 (Allowed): exact memory attribution ---------------------
1.1 %           # C21: sum(domains) vs RSS divergence
1 gb            # C21: durable fill size of the measured row

# --- ledger C8 (Allowed): LFU hit-rate parity ---------------------------
96.19 %         # C8: "96.19% vs 96.19% (+0.00 pp)" — landing-page stat tile cites the allowed wording

# (the historical Vortex post-mortem block was removed 2026-08-10 with the
#  vortex article — no vortex content remains on the site)

# --- ledger C14 (Allowed): cold-boot re-run -----------------------------
7.3 s           # C14: the S08 read-ahead re-run recorded in the Allowed row's evidence cell

# (C19's "≈ 10–11%" tokens were removed 2026-08-11: the S24 re-read did not
#  reproduce the range — drive-state per readiness F20 — and the row
#  reverted to Evidence-pending, so the number came off the site)
10 %            # the divergence gate value named in Allowed rows C21/C27 ("10% fails CI")

# --- ledger C21 (Allowed): attribution measured value -------------------
1.04 %          # C21: measured divergence on the re-validated row (1.1% is the allowed bound wording)

# --- ledger C24 (Allowed): idoc density ---------------------------------
1.108 x         # C24: aggregate density vs msgpack
0.764 x         # C24: aggregate density vs JSON text

# --- ledger C26 (Allowed): delta log volume -----------------------------
0.066 x         # C26: delta records vs full-document log volume, 64-op mix

# --- ledger C27 (Allowed): document attribution -------------------------
0.056 %         # C27: attribution vs RSS divergence, 65k-document load

# --- non-claims (documented) ---------------------------------------------
16 gb           # config example in the INF.NS docs (MAXMEMORY 16gb) — master plan §4.2 example, not a measurement
16 mib          # record-format value bound (SETRANGE note in the compat matrix; value bound on the operations limitations list) — wire format, not a measurement
256 mib         # config defaults (--segment-bytes / --ckpt-interval-bytes) on the deployment page — configuration values, not measurements
2048 mb         # config example in the landing terminal (INF.NS ... MEM-BUDGET 2048mb) — a knob value, not a measurement

# --- v0.4.0-alpha campaign, walked 2026-08-16 ---------------------------
# --- ledger C31 (Allowed): 24 h memory flatness, three planes -----------
0.234 %         # C31: measured RSS slope per 24 h
0.5 %           # C31: the gate value, disclosed alongside the measurement
0.016 %         # C31: accounted-memory slope (the hard sub-gate)
# --- ledger C33 (Allowed): tiered write amplification -------------------
1.89 x          # C33: measured worst-namespace write amplification
3 x             # C33: the gate value, disclosed alongside the measurement
# --- ledger C29 (Allowed): document memory vs RedisJSON -----------------
0.38 x          # C29: measured resident memory vs RedisJSON, frozen corpus
0.7 x           # C29: the gate value, disclosed alongside the measurement
1.04 x          # C29: measured resident memory vs serialized size
# --- ledger C40 (Allowed): tiered fault-injection safety ----------------
20000 seeds     # C40: randomized simulation seeds, two scenarios, 0 violations
                # (the soft cross-check warns because the ledger writes it "20,000")
# --- ledger C38a (Allowed): tiered node restart -------------------------
5.9 s           # C38a: measured 10 GB tiered node boot to serving
# --- ledger C38c (Allowed): three-plane memory attribution --------------
6.5 %           # C38c: worst sum(domains)-vs-RSS divergence, tiered leg live
# --- ledger C28a (Allowed): document read latency vs GET ----------------
1.13 x          # C28a: measured 1 KiB document read p50 vs plain GET
1.44 x          # C28a: measured 200 B document read p50 vs plain GET
1.5 x           # C28a: the gate value, disclosed alongside the measurement
# --- ledger C39 (Allowed): document write throughput --------------------
1.28 m ops/s    # C39: measured JSON.SET throughput at 1 KiB (standalone claim)

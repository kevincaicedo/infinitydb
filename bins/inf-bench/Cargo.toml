[package]
name = "inf-bench"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "InfinityDB benchmark harness: env-check, native RESP loadgen, gate-report generator"

# Tooling tier: no external dependencies (ADR-0134). Internal primitives are
# shared with the engine; the client-side RESP implementation is independent.
[dependencies]
inf-foundation.workspace = true

[lints]
workspace = true

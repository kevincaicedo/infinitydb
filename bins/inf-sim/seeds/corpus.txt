# DST seed corpus (M1-S15). Format: `<scenario> <seed>` per line.
#
# Contract: every line replays green on every merge to main (the corpus
# test) and at full size in the nightly fleet. When a nightly fresh seed
# fails, the fix lands WITH that seed appended here — the corpus is the
# fossil record of every bug the fleet ever caught.
m0-smoke 0xC0FFEE
m0-smoke 0xBEEF
m0-smoke 0x5EED
m1-cache 0xCAFE
m1-cache 0xC0FFEE
m1-cache 0xD15EA5E
m2-durable 0xC0FFEE
m2-durable 0x5EED005D
m2-durable 0x5EED0003
# M2.5-S14: the unified durable+memory+pub/sub+expiry shape (0x5EED0003 is a
# double-cut seed) and the boot-storm wedge oracle.
m2-combined 0xC0FFEE
m2-combined 0x5EED0003
boot-storm 0xB007
boot-storm 0x5EED0003

# DST seed corpus (M1-S15). Format: `<scenario> <seed>` per line.
#
# Contract: every line replays green on every merge to main (the corpus
# test) and at full size in the nightly fleet. When a nightly fresh seed
# fails, the fix lands WITH that seed appended here — the corpus is the
# fossil record of every bug the fleet ever caught.
m0-smoke 0xC0FFEE
m0-smoke 0xBEEF
m0-smoke 0x5EED
m1-cache 0xCAFE
m1-cache 0xC0FFEE
m1-cache 0xD15EA5E
m2-durable 0xC0FFEE
m2-durable 0x5EED005D
m2-durable 0x5EED0003
# M3-S18: multi-match document delta + cadence full + checkpoint cuts.
m3-document 0xD0C00018
# M3-S23/S24: merge-heavy + fuzz-corpus workload with the equivalence
# oracle armed; 0xD0C01803 is a double-cut seed.
m3-document 0xD0C02400
m3-document 0xD0C01803
# M2.5-S14: the unified durable+memory+pub/sub+expiry shape (0x5EED0003 is a
# double-cut seed) and the boot-storm wedge oracle.
m2-combined 0xC0FFEE
m2-combined 0x5EED0003
boot-storm 0xB007
boot-storm 0x5EED0003
# M4-S26: the command-driven tiered node — 0x5EED0003 is a double-cut
# seed whose phase-2 traffic completes with pre-cut flushed tier bytes
# (recovery composes MANIFEST v2 → tier files → checkpoint → WAL tail).
m4-tiered 0x5EED0003
# Group 0 (review of 2026-08-30, §5.5 item 3): adversarial key/value
# lengths — MAX_KEY_LEN-edge keys, values past the 16,368 B cold-window
# class, MSET/MSETNX with over-bound pairs (ADR-0098 atomicity).
m0-adversarial 0xC0FFEE
m0-adversarial 0xBAD5EED
# F-L17-14 (review of 2026-08-30): the M4.5 index crash scenarios ran in
# no automated lane between story close and this row.
m45-backfill 0xC0FFEE
m45-sidecar 0xC0FFEE
# F-L19-05 / F-L19-06 (review of 2026-08-30): the surface mix — SELECTed and
# namespace-bound clients, concurrent SCAN walks, quiescent audits of the
# served surface and the stored content (values, deadlines), seeded flushes.
m0-surface 0xC0FFEE
m0-surface 0x5CA1AB1E

//! `infinity-embedded` — InfinityDB workspace crate (see master plan §20).
//!
//! Stub at M0: this crate's milestone has not started; it exists so the
//! dependency DAG and boundaries are enforced before code arrives.
#![forbid(unsafe_code)]
